namespace peregrine.internal.flatbuf;

// `ChunkHeader` is used to serialize `ChunkMetadata` in chunk.h.
// They must be kept in sync.
struct ChunkHeader {
  handle:uint32;
  buffer:uint32;
  nchunks:uint32;
  index:uint32;
  addr:uint64;
  size:uint32;
}
